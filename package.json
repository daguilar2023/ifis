{
  "name": "ifis-web",
  "private": true,
  "version": "1.0.0",
  "description": "IFIS Consultores website with publications admin",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed": "node scripts/seed-publications.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
